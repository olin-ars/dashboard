// This file contains a dictionary of sidebar modes/configurations. Editing it will change which
// sidebar components/panes appear on each page of the app. Note that the page must still trigger
// a Redux action to switch states upon loading.

const SidebarModes = {
  LOADING: {
  },
  MAP_VIEW: {
  },
};

export default SidebarModes;
